as yet unclassified
displayString: aString at: aPoint on: aForm
  displayStringCo
    _env0 callWith:
      {aString.
      (aPoint x).
      (aPoint y).
      (aForm bits).
      (aForm width).
      (aForm height)}
