private
anyProcessesAbove: highestPriority
    "This method could probably be implemeneted by creating an _allProcessesSelect in env0
    and calling that to replace the allInstances this method currently uses in squeak"
	self notYetImplemented
